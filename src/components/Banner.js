import "./styling.css"

function Banner() {
    return(
        <header>
            <h1>Orbit Report</h1>
            <p>Please click the buttons to see the satellites in that orbit type.</p>
        </header>
    );
};

export default Banner;